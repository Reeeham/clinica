[build]
  command = "npm run build"
  publish = "apps/dashboard/.next"

[build.environment]
  NODE_VERSION = "20"
  # Set NEXT_PUBLIC_API_URL in Netlify dashboard → Site settings → Environment variables

[[plugins]]
  package = "@netlify/plugin-nextjs"
